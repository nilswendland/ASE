import java.time.LocalDate;

public class AnalysisAction {
    int tasksDue = 0;
    int TasksNew=0;
    int TasksInProgress=0;
    int TasksWaiting=0;
    int TasksDone=0;
//    int numberOfTasks = TaskController.getTaskList().size();TaskController.taskList.forEach((title,task)->
  //  {
   //     if (TaskController.getTaskList().getDueDate().isBefore(LocalDate.now())) {
   //         tasksDue++;
   //     }
  //  }  
}
